
Check TFE health status:

ssh ubuntu@${tfe_hostname} -t "sudo docker exec terraform-enterprise-tfe-1 tfe-health-check-status"

Get admin token:

export TFE_ADMIN_URL=$(ssh ubuntu@${tfe_hostname} -t "sudo docker exec -it terraform-enterprise-tfe-1 tfectl admin token --url")
echo $TFE_ADMIN_URL

Copy the echo command output and paste it on your browser.

